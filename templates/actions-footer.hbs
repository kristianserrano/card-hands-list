<div>
    <hr />
    <footer class="sheet-footer flexrow">
        {{#each actionButtons}}
        {{#if display}}
        {{{button.outerHTML}}}
        {{/if}}
        {{/each}}
    </footer>
</div>
